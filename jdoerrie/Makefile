rel:
	mkdir -p out/Release/
	cd out/Release && cmake -DCMAKE_BUILD_TYPE=Release ../../ && make

dbg:
	mkdir -p out/Debug/
	cd out/Debug && cmake -DCMAKE_BUILD_TYPE=Debug ../../ && make

clean:
	rm -rf out/
